幂等性是同一请求执行一次或多次结果一致；例如对同一订单号重复调用支付状态查询GET接口返回同一状态。
TCP面向连接且可靠有序传输，UDP无连接且不保证到达顺序；实时音视频常用UDP，文件传输常用TCP。
数据库索引是加速定位数据的数据结构；通过减少全表扫描、降低I/O来提升查询效率。
哈希冲突是不同键映射到同一槽位；常见解决方式有拉链法和开放寻址法。
容器共享宿主机内核、启动快且轻量；虚拟机含完整Guest OS、隔离更强但资源开销更大。
负载均衡是将流量分发到多台实例以提升可用性与吞吐；如Nginx将请求分发到多个API节点。
最终一致性表示数据副本允许短暂不一致，但在一定时间后会收敛到一致状态。
死锁四个必要条件：互斥、占有且等待、不可剥夺、循环等待。
JWT通常由Header、Payload、Signature三部分组成。
回归测试用于验证改动未破坏已有功能；冒烟测试是快速验证核心流程是否可用。
O(n log n)常见于高效排序，数据量翻倍时时间增长超过线性但显著低于O(n^2)。
缓存击穿是热点Key失效瞬间大量请求打到后端；可用互斥锁、不过期+异步刷新、热点预热缓解。
401表示未认证或认证无效，403表示已认证但无权限访问。
可观测性三大支柱是日志、指标、链路追踪。
A/B测试是对照实验；核心指标常见转化率、留存率、点击率、ARPU及统计显著性。
分布式系统需要全局唯一ID以避免冲突，支持分库分表、排序追踪与幂等处理。
ACID分别是原子性、一致性、隔离性、持久性。
布隆过滤器可高效判断元素可能存在或一定不存在；局限是有误判且不支持精确删除（标准实现）。
灰度发布可降低一次性全量上线风险，便于监控指标并快速回滚。
读扩散是一次写入导致多副本读取路径增加；写扩散是一次逻辑写触发多处物理写入。
def dedup_sort(nums): return sorted(set(nums))，示例 dedup_sort([3,1,3,2])->[1,2,3]。
def binary_search(a,t): l,r=0,len(a)-1; while l<=r: m=(l+r)//2; ... 找到返回索引，否则-1；给出命中、左边界、不存在三类用例。
def char_count(s): 逐字符lower后计数到dict并返回，例如"AaB"=>{"a":2,"b":1}。
实现固定容量LRU：get命中返回值并刷新顺序，put更新/插入并在超容时淘汰最久未使用项。
遍历字符，遇到大写前插入下划线并转小写，如"userNameID"->"user_name_id"。
双指针合并两个有序数组，逐步比较append，最后拼接剩余元素。
用栈匹配括号，左括号入栈，右括号校验栈顶映射，最终栈空即有效。
用哈希表统计频次后按频次降序取前k个元素。
用ThreadPoolExecutor提交下载任务，封装重试和结果收集，伪请求函数sleep后返回内容。
先按行切分CSV，首行作表头，后续行按逗号拆分并zip成字典列表。
解析每行日期字段（如YYYY-MM-DD），用字典累计计数并返回按日期聚合结果。
计算点积与两向量范数，任一范数为0则返回0.0，否则返回dot/(norm1*norm2)。
先过滤非字母数字并统一小写，再判断处理后字符串是否等于其反转。
装饰器在调用前后记录time.perf_counter()，打印毫秒耗时并返回原函数结果。
用Kahn算法：统计入度、队列初始化入度0节点，依次出队并降低邻居入度得到拓扑序。
遍历数组并用哈希表存补数，命中时返回两数下标。
封装可调用任务，失败时按指数退避等待并最多重试5次，最终抛出最后异常。
统计字符频次后比较两个字符串频次字典是否相等。
递归遍历嵌套字典，使用parent.child路径拼接键并写入新字典。
按行扫描文本，按点分4段并校验0-255后收集合法IPv4。
输出至少10轮架构师-新同事问答，涵盖拆分边界、数据迁移、双写校验、灰度与回滚。
输出至少10轮产品经理-后端对话，包含需求澄清、接口定义、特征服务、验收指标。
输出至少10轮SRE-开发复盘，明确时间线、根因定位、容量问题与行动项闭环。
输出至少10轮面试问答，覆盖短链生成、重定向、冲突处理、统计与高可用。
输出至少10轮客服-用户对话，给出核单、补偿、工单时限和升级路径。
输出至少10轮发布讨论，包含测试清单、风险分级、灰度比例与回滚阈值。
输出至少10轮数据工程师-分析师对话，统一埋点口径、字典版本、校验规则。
输出至少10轮站会对话，列出延期风险、阻塞项、负责人和时间承诺。
输出至少10轮联调对话，区分401与403成因并落实鉴权链路排查。
输出至少10轮安全应急对话，包含漏洞分级、临时缓解、补丁发布与复盘。
输出至少10轮DBA-后端对话，包含慢SQL定位、索引方案、回归验证。
输出至少10轮写作者-开发者对话，产出部署手册目录、前置条件和回滚章节。
输出至少10轮Code Review对话，讨论缓存一致性、失效策略、压测与可观测性。
输出至少10轮业务-算法对话，覆盖CTR下降诊断、特征漂移、实验设计。
输出至少10轮运维-网络对话，含链路监控、丢包定位、跨机房优化措施。
输出至少10轮运营-数据科学对话，提出留存假设、分群实验与指标口径。
输出至少10轮师生对话，比较递归/迭代思路、复杂度与典型案例。
输出至少10轮维护者-贡献者对话，明确Issue模板字段与PR检查项。
输出至少10轮融资演练问答，聚焦技术壁垒、成本结构、扩展性与风险。
输出至少10轮售前沟通，覆盖私有化架构、合规要求、验收里程碑。
按需求、约束、数据流、接口、容错、监控六步给出订单系统设计并说明每步理由。
按接入层、缓存、数据库、消息队列、限流降级分层分析秒杀瓶颈与优化取舍。
给出降延迟方案：基线假设、实验设计、指标验证、灰度发布、可回滚策略。
分层建模多租户权限：角色-资源-动作-策略-审计，给出评估流程。
比较MySQL/PostgreSQL/ClickHouse在OLTP与OLAP的特点并给选型结论。
围绕日志成本提出采样、冷热分层、压缩归档、索引优化与查询加速组合方案。
设计可回放调度系统，说明任务ID幂等、去重键、补偿流程与审计追踪。
从威胁建模分析登录系统：攻击面、风险优先级、防护与监控告警。
给出单机推荐到近实时服务的步骤：特征流、在线推理、一致性与回退。
用根因树分析协作低效，并给出可量化改进动作与验证周期。
设计高可用MQ消费端：重试策略、死信队列、顺序保证、监控告警指标。
给出从单体到模块化单体再到微服务的迁移里程碑与治理要点。
评估RAG在客服中的收益与风险，定义上线门槛如准确率、幻觉率、成本。
分析主从延迟事故并给出短中长期治理：读写路由、一致性策略、架构升级。
设计A/B平台：流量分桶、指标体系、统计显著性、止损与自动回滚。
建立工程质量度量体系：代码质量、测试覆盖、发布稳定性、故障恢复指标。
分析首屏慢优化顺序：网络传输、关键渲染路径、资源拆分、缓存命中。
制定Oncall轮值机制：排班公平、SLA响应、复盘沉淀、心理健康保障。
设计审计变更追踪系统：事件模型、检索维度、留存周期与合规策略。
建立技术选型决策流程：评分维度、试点验证、成本收益、退出机制。