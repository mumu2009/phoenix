# v5.2 TODOs
## 0. v5.1残余任务（已完成，见 v5.1_carryover_closure.md）
- 1.完善所有的测试，保证测试的100%覆盖和通过，注意获取真实的数据而不是通过单纯的post/get进行网关通断的测试。
- 2.建立一套完整的性能评估体系，目前已经放置在/test/下，接下来需要对三个评估体系进行补充完善
- 3.通过cli-anything工具，将outsides/中的其他工具转换为可以被ai调用的cli工具,这其实十分重要，因为它是ai的数学底座。
## 1. 文档与定义统一（已完成，见 glossary.md / io_spec.md / api.md）
- 明确“词语”“词元”“词素”“语义最小单位”的正式定义，并统一在全部代码与文档中的术语用法。
- 为转换算法补充输入输出规范，包括 GGUF 权重、词表、分词映射、神经动力学参数表的格式定义。
- 补充 v5.2 API 文档，列出新增接口、参数、返回值、异常与兼容性策略。

## 2. 算法实现
- 已落地补口：网关侧已新增 GGUF 本地后端监督器，会为 `llamacpp/bitnet` 自动生成脑图映射清单并把状态暴露到监控接口。
- 已落地补口：数学底座已可优先走内建求值，并在复杂命令下桥接 `outsides/_calculator`，为后续 cli-anything 统一封装留出接口。
- 已完成：GGUF 权重读取与解析已落地；未完成部分仅剩“结构化导出模块”本身。
- 部分完成：词级到词元级的语义拆分与合并已有参数与运行时占位，但算法内核未闭环。
- 未完成：Transformer 权重到脉冲/神经动力学参数的完整映射模块。
- 部分完成：梯度下降参数拟合已有占位字段，但缺乏独立通用拟合实现与收敛验证。
- 未完成：滑动窗口权重按需装载机制，当前仅有窗口裁剪与调度参数，不是 page-in/page-out 式加载。
- 已完成：人格库调度器基础设施已落地，支持多进程策略参数与分时复用控制；仍可继续优化调度策略。

## 3. 正确性验证
- 部分完成：等价性测试已具备问答与 benchmark 基础链路，但分类、检索、工具调用四类统一测试集尚未完全成型。
- 未完成：词级、词元级和混合粒度三种分词策略的系统化消融实验。
- 未完成：激活函数参数拟合收敛曲线验证。
- 未完成：滑动窗口机制的上下文保持验证。
- 部分完成：人格库调度已有运行时与自治控制，但一致性与可重复性实验仍未独立成文。

## 4. 性能与资源评估
- 已落地补口：`Database079` 新增了 Redis 热层 / SQLite 冷层的访问统计与重平衡，监控项可直接观察 promotion/demotion 行为。
- 部分完成：system API 与直连 Ollama 的延迟、吞吐、质量对比已落地；原始 GGUF 与“转换模型”的严格 A/B 仍未完整成文。
- 未完成：混沌演算近似方案在不同误差阈值下的系统化评估。
- 部分完成：数学桥接与外部工具调用已可跑通，但 cli-anything 统一封装后的失败率与重试成本评估未独立完成。
- 未完成：长上下文滑动窗口大小对性能与准确率的系统化基准。

## 5. 风险与边界
- 已落地补口：`bug_shooter` 独立监控进程已接入主程序挂载路径，并在超阈值时执行 working set trim，状态写入 `runtime_store/bug_shooter_status.json`。
- 未完成：LoRA 难以直接转换的条件分析与回退策略。
- 未完成：词语原子性牺牲后的语义漂移与歧义放大评估。
- 未完成：神经动力学参数物理边界检查。
- 部分完成：已有超时与独立进程运行控制，但完整沙箱、权限边界与审计日志尚未完成。

## 6. 发布准备
- 已完成：测试覆盖率统计方法已整理，见 `coverage.md`。
- 已完成：最小可复现实验脚本已补充，见 `test-tools/repro_v52_full.bat`。
- 已完成：v5.2 发布说明已补充，见 `release.md`。